#pragma once

#define NULL        0
#define NULLPTR     0

#define FALSE       0
#define TRUE        1

#define max(a, b) (a > b ? a : b)
#define min(a, b) (a < b ? a : b)

#define abs(x) ((x) >= 0 ? (x) : -(x))

#define SUCCESSFUL(x) ((x) >= 0)
